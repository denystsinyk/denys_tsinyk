---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/components/HeroSection.tsx
  - src/components/WorkSection.tsx
  - src/components/Footer.tsx
  - src/data/work.ts
  - src/data/social.ts
  - public/favicon.ico
autonomous: true
requirements:
  - FNDN-01
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - SOCL-01

must_haves:
  truths:
    - "Running `npm run dev` starts a dev server at localhost:5173 with no errors"
    - "The page renders 'Denys Tsinyk' as a prominent h1 header with a headshot alongside it"
    - "Three work experience one-liners appear: NFL, Pitt, PittCSC — with no dates"
    - "A footer renders four icon links: GitHub, LinkedIn, Email, Steam"
    - "The page background is #0a0a0a (dark), text is #ffffff, and green (#00ff00) accent is present as a CSS variable"
    - "TypeScript compilation produces zero errors (`npm run build` succeeds locally)"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite build config with Tailwind v4 plugin and dynamic base path"
      contains: "VITE_BASE_PATH"
    - path: "src/index.css"
      provides: "Tailwind v4 entrypoint with design tokens"
      contains: "@import \"tailwindcss\""
    - path: "src/components/HeroSection.tsx"
      provides: "Name + headshot header component"
      contains: "import.meta.env.BASE_URL"
    - path: "src/components/WorkSection.tsx"
      provides: "3 work experience one-liners"
    - path: "src/components/Footer.tsx"
      provides: "Social icon links footer"
    - path: "src/data/work.ts"
      provides: "Static work experience array"
    - path: "src/data/social.ts"
      provides: "Static social links array with Steam ID"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/HeroSection.tsx"
      via: "import and render"
      pattern: "import.*HeroSection"
    - from: "src/components/HeroSection.tsx"
      to: "public/headshot.jpg"
      via: "import.meta.env.BASE_URL prefix"
      pattern: "import\\.meta\\.env\\.BASE_URL"
    - from: "src/components/Footer.tsx"
      to: "src/data/social.ts"
      via: "socialLinks import"
      pattern: "import.*socialLinks"
---

<objective>
Scaffold the complete Vite 6 + React 18 + TypeScript 5 + Tailwind CSS v4 project and author all static content components — hero, work experience, and social footer — so the site renders correctly in local dev.

Purpose: Establishes the full project foundation before the deployment pipeline is added. A working local build proves all content and styling is correct before CI is introduced.
Output: A fully functional local development build with static portfolio content, correct dark aesthetic, and zero TypeScript errors.
</objective>

<execution_context>
@C:/Users/denys/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denys/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript + Tailwind v4 project</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    tsconfig.app.json
    tsconfig.node.json
    index.html
    src/main.tsx
    src/App.tsx
    src/index.css
  </files>
  <action>
    Run inside C:/Users/denys/Desktop/denys_tsinyk (the existing git repo — NOT a parent directory):

    ```bash
    cd C:/Users/denys/Desktop/denys_tsinyk
    npm create vite@latest . -- --template react-ts
    # When prompted "Current directory is not empty. Remove existing files and continue?" — choose "Ignore files and continue" (preserves .git, README.md, .planning/)
    npm install
    npm install tailwindcss @tailwindcss/vite
    npm install react-icons
    ```

    After scaffolding, update `vite.config.ts` to:
    - Import `tailwindcss` from `@tailwindcss/vite`
    - Add `tailwindcss()` to the plugins array
    - Set `base: process.env.VITE_BASE_PATH ?? '/'`
    - Do NOT add postcss.config.js — Tailwind v4 uses the Vite plugin only

    Replace `src/index.css` entirely with:
    ```css
    @import "tailwindcss";

    :root {
      --color-bg: #0a0a0a;
      --color-accent: #00ff00;
      --color-text: #ffffff;
      --color-divider: #1a1a1a;
      --font-mono: 'JetBrains Mono', 'Fira Code', ui-monospace, monospace;
      --font-sans: 'Inter', system-ui, sans-serif;
    }

    body {
      background-color: var(--color-bg);
      color: var(--color-text);
      font-family: var(--font-sans);
      min-height: 100vh;
      margin: 0;
    }
    ```

    Update `index.html` `<head>` to add Google Fonts link for JetBrains Mono and Inter:
    ```html
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
    ```
    Also update `<title>` to "Denys Tsinyk".

    Replace `src/App.tsx` with a minimal shell (single `<main>` element with `className="max-w-2xl mx-auto px-6 py-12"`) — components will be wired in Task 2.

    Remove Vite's default boilerplate: delete `src/assets/react.svg` and `public/vite.svg` if they exist. Verify `public/favicon.ico` is present (Vite template includes one).

    **Critical:** Do NOT create `src/types/data.ts`, `src/hooks/useData.ts`, or any `scripts/` directory — those are Phase 2 additions.
  </action>
  <verify>
    <automated>cd C:/Users/denys/Desktop/denys_tsinyk && npm run build 2>&1 | tail -5</automated>
    <manual>Confirm `dist/` is created and `dist/index.html` exists after build</manual>
    <sampling_rate>run after this task, before Task 2</sampling_rate>
  </verify>
  <done>
    `npm run build` exits 0 with no TypeScript errors. `npm run dev` starts without errors. `vite.config.ts` contains `VITE_BASE_PATH` and `tailwindcss()` plugin. `src/index.css` contains `@import "tailwindcss"` and the `:root` design tokens. No `postcss.config.js` exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author static content components (HeroSection, WorkSection, Footer) and data files</name>
  <files>
    src/data/work.ts
    src/data/social.ts
    src/components/HeroSection.tsx
    src/components/WorkSection.tsx
    src/components/Footer.tsx
    src/App.tsx
  </files>
  <action>
    Create `src/data/work.ts`:
    ```typescript
    export interface WorkExperience {
      role: string
      org: string
      description: string
    }

    export const workExperience: WorkExperience[] = [
      { role: 'Software Engineer Intern', org: 'NFL', description: 'Security automation' },
      { role: 'Teaching Assistant', org: 'Pitt', description: 'Data Structures & Algorithms' },
      { role: 'Events Coordinator', org: 'PittCSC', description: '' },
    ]
    ```

    Create `src/data/social.ts`:
    ```typescript
    export interface SocialLink {
      label: string
      url: string
      icon: 'github' | 'linkedin' | 'email' | 'steam'
    }

    export const socialLinks: SocialLink[] = [
      { label: 'GitHub', url: 'https://github.com/GITHUB_USERNAME', icon: 'github' },
      { label: 'LinkedIn', url: 'https://linkedin.com/in/LINKEDIN_SLUG', icon: 'linkedin' },
      { label: 'Email', url: 'mailto:EMAIL_ADDRESS', icon: 'email' },
      { label: 'Steam', url: 'https://steamcommunity.com/profiles/76561198275331284', icon: 'steam' },
    ]
    ```
    Note: GitHub username, LinkedIn slug, and email address are placeholder strings. The developer must replace GITHUB_USERNAME, LINKEDIN_SLUG, and EMAIL_ADDRESS with real values before going live.

    Create `src/components/HeroSection.tsx`:
    - Flex row layout with gap between headshot and text
    - `<img src={`${import.meta.env.BASE_URL}headshot.jpg`} alt="Denys Tsinyk" />` — use `import.meta.env.BASE_URL` prefix (NOT absolute `/headshot.jpg` — that 404s on GitHub Pages)
    - Image: w-24 h-24 rounded-full object-cover; include `onError` handler that hides the image gracefully if headshot.jpg is not yet placed in public/
    - `<h1>` with "Denys Tsinyk" — text-4xl font-bold tracking-tight, color: var(--color-text)
    - Optional tagline line beneath name (e.g., "Software Engineer") in text-neutral-400 text-sm

    Create `src/components/WorkSection.tsx`:
    - Import `workExperience` from `../data/work`
    - Section with `borderTop: '1px solid var(--color-divider)'` separator
    - Map over `workExperience` — render each as a single line: "[role] @ [org]" with description appended after em dash if non-empty
    - No dates rendered anywhere
    - Text small, color var(--color-text); description portion in text-neutral-400

    Create `src/components/Footer.tsx`:
    - Import FaGithub, FaLinkedin, FaSteam from 'react-icons/fa'; MdEmail from 'react-icons/md'
    - Import `socialLinks` from `../data/social`
    - `iconMap` object maps icon key to component
    - Flex row of icon links; borderTop: '1px solid var(--color-divider)'
    - Each `<a>` has: correct href, aria-label, target="_blank" + rel="noopener noreferrer" for all except email
    - Icon size text-xl or text-2xl; hover:opacity-70 transition-opacity

    Update `src/App.tsx` to:
    - Import and render HeroSection, WorkSection, Footer in order
    - Wrap in `<div style={{ backgroundColor: 'var(--color-bg)', minHeight: '100vh' }}>`
    - Inner container: `<main className="max-w-2xl mx-auto px-6 py-12">`
    - HeroSection and WorkSection inside main; Footer inside main after WorkSection
    - Pass `name="Denys Tsinyk"` prop to HeroSection
  </action>
  <verify>
    <automated>cd C:/Users/denys/Desktop/denys_tsinyk && npm run build 2>&1 | tail -10</automated>
    <manual>Run `npm run dev`, open browser at http://localhost:5173 — confirm: "Denys Tsinyk" header visible, 3 work entries visible, 4 footer icons visible, background is dark (#0a0a0a)</manual>
    <sampling_rate>run after this task completes</sampling_rate>
  </verify>
  <done>
    `npm run build` exits 0 with zero TypeScript errors. In local dev: name header renders, 3 work experience one-liners appear (no dates), footer shows 4 icons with correct Steam link to profile 76561198275331284. Background is dark. Headshot renders if public/headshot.jpg exists, or gracefully hides if not yet placed.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd C:/Users/denys/Desktop/denys_tsinyk && npm run build` — must exit 0 with zero TypeScript errors
2. `npm run dev` — open http://localhost:5173 and visually confirm all content sections
3. Confirm `vite.config.ts` has `base: process.env.VITE_BASE_PATH ?? '/'` (not hardcoded repo name)
4. Confirm no `postcss.config.js` file exists
5. Confirm `src/index.css` uses `@import "tailwindcss"` (not `@tailwind base/components/utilities`)
6. Confirm headshot `<img>` uses `import.meta.env.BASE_URL` prefix
</verification>

<success_criteria>
- `npm run build` exits 0 with zero TypeScript errors
- Local dev server renders "Denys Tsinyk" prominently as h1
- All 3 work experience entries visible (NFL, Pitt, PittCSC) — no dates
- Footer has 4 icon links including Steam pointing to profile 76561198275331284
- Dark background (#0a0a0a) and CSS custom properties visible in browser DevTools
- Project structure matches the Phase 1 architecture from RESEARCH.md (no Phase 2 files created)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- What was scaffolded (exact package versions from package.json)
- Patterns used (Tailwind v4 import, BASE_URL image path, design tokens)
- Placeholder values still needing real data (GitHub username, LinkedIn slug, email)
- Whether headshot.jpg was placed in public/ or left as placeholder
- Any deviations from the plan
</output>
