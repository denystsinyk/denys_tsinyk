---
phase: 05-polish-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - GAME-04

must_haves:
  truths:
    - "Game card hover scale-up and green glow are visually smooth at 150ms"
    - "PLAYING NOW badge and pulse dot are invisible when data is stale (>30 min old)"
    - "GAMES heading shows the Steam icon"
    - "Game card row has no visible scrollbar"
    - "Content column is visibly wider on desktop"
    - "No horizontal page overflow at any common viewport width"
    - "At mobile widths (~375px) approximately 1.5 game cards are visible"
    - "GitHub contribution count is rendered in monospace font"
  artifacts:
    - path: ".planning/phases/05-polish-hardening/05-01-SUMMARY.md"
      provides: "Confirmed completion record for Plan 01"
  key_links: []
---

<objective>
Human visual verification of all Phase 5 polish changes applied in Plan 01.

Purpose: Confirm that hover effects, staleness suppression, responsive layout, and monospace typography are all visually correct before marking the phase complete.

Output: Approval signal from user; if issues found, documented feedback for gap closure.
</objective>

<execution_context>
@C:/Users/denys/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denys/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-hardening/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of all Phase 5 polish changes</name>
  <files>none — visual check only</files>
  <action>Human verification step: run the dev server and confirm all Phase 5 polish changes render correctly.</action>
  <what-built>
    All Phase 5 polish changes from Plan 01:
    - Game card hover: scale 1.04 + green glow on enter; restore correctly on leave (including playing-glow restore)
    - Staleness suppression: PLAYING NOW badge + pulse dot hidden when data older than 30 min
    - GAMES heading: FaSteam icon beside "GAMES" text
    - Game card row: no visible browser scrollbar
    - Layout width: content column widened to ~896px max (max-w-4xl) from ~672px (max-w-2xl)
    - Responsive: game cards wider on mobile for ~1.5-card peek effect at 375px
    - Monospace: GitHub contributions count in monospace font
  </what-built>
  <how-to-verify>
    Run the dev server first: `npm run dev` in the project root.

    **1. Layout width (desktop)**
    - Visit http://localhost:5173
    - Content column should noticeably fill more horizontal space than before (~896px vs ~672px)
    - No horizontal scrollbar on the page itself

    **2. GAMES section heading icon**
    - Scroll to GAMES section
    - Steam icon should appear to the left of "GAMES" text (similar to how Spotify icon appears beside "RECENTLY PLAYED")

    **3. Game card scrollbar**
    - Scroll the game card row horizontally
    - No browser scrollbar should be visible below the row (it was previously visible)

    **4. Game card hover effect**
    - Hover over any game card
    - Should see: slight scale-up (1.04x — subtle lift), green glow around the card edges
    - Transition should be snappy (150ms) — not sluggish
    - Mouse off: card returns to normal size; if it was a PLAYING NOW card, the green glow should remain (not disappear)

    **5. Staleness suppression** (may need to simulate)
    - In public/data.json, temporarily set `updated_at` to a timestamp more than 30 minutes ago (e.g., `"2026-01-01T00:00:00.000Z"`)
    - Reload the page — any PLAYING NOW badge and pulse dot should be gone; the card is still visible
    - Restore updated_at to current time after verifying

    **6. Mobile layout**
    - Open DevTools, set viewport to 375px width (iPhone SE size)
    - Game card row should show ~1.5 cards (one full card + partial edge of second card visible)
    - Music section snap-scroll row should also be functional
    - No horizontal overflow on the page (no sideways page scroll)

    **7. GitHub monospace**
    - Scroll to GitHub Stats section
    - The contribution streak number should be in monospace (already was)
    - The "X contributions this year" count value (the number) should now also be in monospace font
  </how-to-verify>
  <verify>All 7 visual checks pass: wider layout, Steam icon in heading, no scrollbar on game row, hover scale+glow works, PLAYING NOW suppressed when stale, ~1.5 cards at 375px, contributions count in monospace.</verify>
  <done>All Phase 5 success criteria visually confirmed — hover, staleness, heading icon, scrollbar, layout width, mobile responsive, monospace typography all correct.</done>
  <resume-signal>Type "approved" if everything looks correct, or describe any visual issues found</resume-signal>
</task>

</tasks>

<verification>
Human approval received via resume-signal.
</verification>

<success_criteria>
User has visually confirmed all Phase 5 polish changes render correctly in the dev server.
Phase 5 is complete — all 22 v1 requirements are satisfied.
</success_criteria>

<output>
After user approval, create `.planning/phases/05-polish-hardening/05-02-SUMMARY.md` following the summary template, then update STATE.md to reflect Phase 5 complete.
</output>
