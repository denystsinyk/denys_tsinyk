---
phase: 04-spotify-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - MUSC-01
  - MUSC-02
  - MUSC-03

must_haves:
  truths:
    - "Spotify secrets are set in GitHub repo settings and the pipeline step runs without error"
    - "The live site shows the Recently Played section with real Spotify track data (album art, names, artists)"
    - "Track cards are horizontally scrollable with snap behavior and no visible scrollbar"
    - "Clicking a track opens the correct Spotify track URL in a new tab"
    - "When spotify_ok is false, the section shows the fallback message instead of blank space"
  artifacts:
    - path: "src/components/MusicSection.tsx"
      provides: "Rendered and visually verified music section"
    - path: "scripts/fetch-spotify-data.js"
      provides: "Confirmed working pipeline script"
  key_links:
    - from: "GitHub Actions refresh-data.yml"
      to: "public/data.json"
      via: "Fetch Spotify data step commits updated spotify data"
      pattern: "spotify_ok.*true"
---

<objective>
Human verification that the complete Spotify integration works end-to-end: the OAuth secrets are configured, the pipeline step runs and commits real data, and the rendered music section displays correctly in the browser.

Purpose: Confirm Phase 4 success criteria are met — live track data visible, clickable tracks, fallback state working.
Output: Confirmed working Spotify pipeline + UI
</objective>

<execution_context>
@C:/Users/denys/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/denys/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-spotify-integration/04-01-SUMMARY.md
@.planning/phases/04-spotify-integration/04-02-SUMMARY.md
@.planning/phases/04-spotify-integration/RUNBOOK-spotify-token.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set up Spotify OAuth secrets and trigger pipeline run</name>
  <files>none — human-only setup in GitHub/Spotify dashboards</files>
  <action>Human-only step: complete Spotify OAuth flow per RUNBOOK-spotify-token.md, add secrets to GitHub repo, trigger pipeline manually and confirm success.</action>
  <what-built>
    The pipeline script (scripts/fetch-spotify-data.js) and workflow step are ready. Before the pipeline can run, you need to complete the one-time Spotify OAuth setup and add three secrets to the GitHub repository.
  </what-built>
  <how-to-verify>
    Follow the runbook: `.planning/phases/04-spotify-integration/RUNBOOK-spotify-token.md`

    Short summary:
    1. Create a Spotify app at https://developer.spotify.com/dashboard (add redirect URI: `http://127.0.0.1:3000/callback`)
    2. Build and open the authorization URL in a browser (see runbook Step 2), click Agree, copy the `code` from the redirect URL
    3. Exchange the code for a refresh_token using the curl command in the runbook (Step 3)
    4. Add these three secrets to GitHub repo → Settings → Secrets and variables → Actions:
       - `SPOTIFY_CLIENT_ID`
       - `SPOTIFY_CLIENT_SECRET`
       - `SPOTIFY_REFRESH_TOKEN`
    5. Manually trigger the workflow: GitHub repo → Actions → Refresh Data → Run workflow
    6. Confirm the "Fetch Spotify data" step in the workflow run log shows success (no error, prints "Spotify data fetched: 5 tracks")
    7. Check the commit created by the workflow — `public/data.json` should have `spotify_ok: true` and 5 entries in `spotify.recent_tracks`
  </how-to-verify>
  <verify>Pipeline run log shows "Spotify data fetched: 5 tracks"; public/data.json has spotify_ok: true with 5 entries in recent_tracks</verify>
  <done>All 3 secrets set in GitHub, pipeline run succeeds, data.json updated with live Spotify data</done>
  <resume-signal>Type "secrets set" when all 3 secrets are added and the pipeline run confirms success, or describe any issues encountered.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of the live music section</name>
  <files>none — visual check only</files>
  <action>Human verification step: visit the live site and confirm the music section renders correctly with real Spotify data.</action>
  <what-built>
    MusicSection is wired into App.tsx with live Spotify data flowing from the pipeline. The site is deployed via the existing deploy.yml workflow.
  </what-built>
  <how-to-verify>
    1. Open the live GitHub Pages URL (or run `npm run dev` locally with a data.json containing real Spotify data)
    2. Scroll to the "Recently Played" section — verify:
       a. The Spotify logo icon appears next to "RECENTLY PLAYED" heading
       b. Up to 5 track cards visible as a horizontal row
       c. Each card shows: square album art, track name below (truncated if long), artist name below that
       d. A partial peek of the rightmost card edge confirms there are more cards to scroll to
    3. Scroll the row — verify cards snap cleanly per card, no visible scrollbar
    4. Click a track card — verify it opens the correct Spotify track URL in a new tab
    5. To test fallback: temporarily set `spotify_ok: false` in `public/data.json` locally and run dev server — verify the section shows "Music data is currently unavailable" at minimal height (no blank space, no layout break)
    6. Restore data.json after fallback test
  </how-to-verify>
  <verify>All 6 visual checks pass: Spotify logo visible, 5 track cards with album art and names, snap scroll works, no scrollbar visible, clicks open Spotify URLs, fallback state confirmed with spotify_ok: false</verify>
  <done>Music section renders correctly with live data; all MUSC-01/02/03 requirements visually confirmed</done>
  <resume-signal>Type "approved" if all checks pass, or describe any visual/functional issues (e.g., "album art not loading", "snap not working", "wrong track URL").</resume-signal>
</task>

</tasks>

<verification>
Phase 4 complete when:
- GitHub Actions "Fetch Spotify data" step runs without errors
- public/data.json has spotify_ok: true with 5 tracks in recent_tracks
- Live site renders the music section with real track data
- Horizontal scroll + snap + hidden scrollbar confirmed working
- Track clicks open correct Spotify URLs in new tab
- Fallback state confirmed functional
</verification>

<success_criteria>
- MUSC-01: User sees last 5 Spotify tracks as horizontal scrollable row with square album art, track name, artist name
- MUSC-02: Each track opens Spotify track URL in new tab when clicked
- MUSC-03: Fallback "Music data is currently unavailable" shown when spotify_ok is false or tracks empty
- Pipeline confirms: Spotify data fetch step runs cleanly in GitHub Actions
</success_criteria>

<output>
After completion, create `.planning/phases/04-spotify-integration/04-03-SUMMARY.md`
</output>
